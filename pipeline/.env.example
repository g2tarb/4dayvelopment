# ── Telegram ──
TELEGRAM_BOT_TOKEN=
TELEGRAM_ADMIN_CHAT_ID=1436812162

# ── Intake (le site POST /api/contact relaie ici) ──
PIPELINE_PORT=4100
PIPELINE_INTAKE_TOKEN=

# ── LLM / PDF (P2+) ──
ANTHROPIC_API_KEY=
PERPLEXITY_API_KEY=
OPENROUTER_API_KEY=
PDFSHIFT_API_KEY=

# ── Email (réutilise les creds du site) ──
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=
MAIL_PASS=
MAIL_FROM=contact@4dayvelopment.fr
# Destinataire des 3 prompts design (à coller dans Claude Design). Défaut : MAIL_TO puis MAIL_USER.
OPERATOR_EMAIL=

# ── Base ──
# PIPELINE_DB_PATH=./data/pipeline.db
